# Tiny configuration for BDH model in MLX (for testing)
model:
  vocab_size: 128
  n_neurons: 1024
  d_model: 64
  n_layers: 2

training:
  batch_size: 8
  learning_rate: 0.001
  num_epochs: 5
  checkpoint_interval: 100

data:
  max_sequence_length: 128